//import { } from '../constants';

const initialState = {
    "firstName": null,
    "lastName": null,
    "email": null,
    "role": null,
    "userName": null
}

export function applicationStore(state = initialState, action) {
    switch (action.type) {

        default:
            return state
    }
}